import React, { useState } from 'react';

const AddSeries = ({ user, logoutUser, apiUrl, seriesList, getSeriesList }) => {

    return (
        <h1>Add series</h1>
    );
}

export default AddSeries;